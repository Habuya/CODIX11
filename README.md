# QuantumCore V2.0 🌌

**Cinematic AI Interface with Quantum-Glass Aesthetics.**

Step into the future of conversational AI. QuantumCore V2.0 combines a highly responsive, glassmorphic UI with a modular JavaScript architecture for maximum performance and maintainability.

## 📁 System Architecture

### Core Logic (`js/`)
*   **`script.js`**: Main orchestration, bootstrapping, and event handling.
*   **`js/audio.js`**: Web Audio API engine (Sound FX, Speech Synthesis/Recognition).
*   **`js/visuals.js`**: Canvas rendering engine (Neural Particles, Matrix Rain).
*   **`js/memory.js`**: Persistence layer (QuantumArchive - LocalStorage/IndexedDB).
*   **`js/utils.js`**: Utility functions (Time, Theme, HUD updates).
*   **`js/auditor.js`**: System integrity and context density analysis (`/status`).

### Visuals (`css/`)
*   **`css/style.css`**: Core styling, Animations (Aurora, Glitch), and Layouts.

## 🚀 Installation & Setup

1.  **Clone the Repository**
    ```bash
    git clone https://github.com/YOUR_USERNAME/QuantumCore.git
    cd QuantumCore
    ```

2.  **Start the Local Server**
    No complex build tools required. Just serve the files.
    ```bash
    npx serve .
    ```

3.  **Initialize**
    Open `http://localhost:3000` and click **INITIALIZE SYSTEM**.

## 🎮 Special Commands
*   `/status` - Show Matrix integrity report.
*   `/visualize` - Toggle knowledge graph overlay.
*   `/reset` - Clear chat history.
*   `/theme` - Toggle visual themes.

## 🔧 Troubleshooting
*   **No Sound?** Ensure you interacted with the page (clicked Initialize) to start the AudioContext.
*   **Loader stuck?** A failsafe will auto-remove it after 6 seconds. Check console for red error boxes.

---
*Generated by Antigravity Agents.*
